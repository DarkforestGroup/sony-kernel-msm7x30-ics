--- mm/oom_kill.c
+++ mm/oom_kill.c
@@ -426,8 +426,6 @@
 	list_for_each_entry(c, &p->children, sibling) {
 		if (c->mm == p->mm)
 			continue;
-		if (mem && !task_in_mem_cgroup(c, mem))
-			continue;
 		if (!oom_kill_task(c))
 			return 0;
 	}
